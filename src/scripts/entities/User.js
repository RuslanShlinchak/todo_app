class User {
  constructor({
    id,
    name,
    email,
    age,
    token
  }) {
    this.id = id;
    this.name = name;
    this.email = email;
    this.age = age;
    this.token = token;
  }
}

export default User;
